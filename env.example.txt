# ============================================================
#  Bing 每日壁纸 — Docker 环境变量模板
#  使用方式: cp env.example.txt .env 然后修改 .env 中的值
# ============================================================

# ---- 是否在容器启动时自动抓取一次 ----
BING_AUTO_FETCH=false

# ---- 定时抓取 (crontab 格式，可选) ----
# 示例: 每天凌晨 3:00 执行
# BING_CRON="0 3 * * *"
BING_CRON=

# ---- Bark (iOS 推送) 配置 ----
# 支持逗号分隔多个设备密钥
BARK_DEVICE_KEYS=your_bark_device_key_here
BARK_API_URL=https://api.day.app/push

# ---- Synology Chat 通知 (可选) ----
# 支持逗号分隔多个 Webhook 地址
SYNOLOGY_CHAT_WEBHOOKS=

# ---- 时区 ----
TZ=Asia/Shanghai